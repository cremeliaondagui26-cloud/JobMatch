# Donnees

- `raw/` contient les donnees sources non modifiees.
- `processed/` contient les donnees nettoyees, enrichies ou transformees.

Conserver les sources originales dans `raw/` et ecrire les sorties de preparation dans `processed/`.
